class Recipe {
  final int id;
  final String name;
  final String description;

  // Recipe(this.id, this.name, this.description, {required int id, required String name, required int id});

  const Recipe({
    required this.id,
    required this.name,
    required this.description,
  });
}
